import { StyleSheet } from 'react-native';

export const stylesListIsEmptyMessages = StyleSheet.create({
  container: {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 8
  },
});
