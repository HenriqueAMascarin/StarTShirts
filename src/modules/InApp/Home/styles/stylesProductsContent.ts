import { StyleSheet } from 'react-native';

export const stylesProductsContent = StyleSheet.create({
  container: {
    flex: 1,
    marginVertical: 30,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'flex-start',
    flexDirection: 'column',
    gap: 30,
  },
});
