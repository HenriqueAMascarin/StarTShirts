import { StyleSheet } from 'react-native';

export const stylesLoadingScreen = StyleSheet.create({
  container: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 30,
    flex: 1,
    marginHorizontal: 'auto'
  },
  text: {
    fontSize: 26,
  },
});
