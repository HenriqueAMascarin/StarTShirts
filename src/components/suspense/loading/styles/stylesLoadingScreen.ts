import { StyleSheet } from 'react-native';

export const stylesLoadingScreen = StyleSheet.create({
  container: {
    display: 'flex',
    alignItems: 'center',
    gap: 30,
  },
  text: {
    fontSize: 26,
  },
});
